version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd ui
        - nvm install 20.11.1
        - nvm use 20.11.1
        - npm ci
        - npm install -g @angular/cli
        - ng add @angular/ssr
    build:
      commands:
        - ng build
  artifacts:
    baseDirectory: ui/dist/fijian-ui/server
    files:
      - '**/*'
  cache:
    paths:
      - ui/node_modules/**/*
