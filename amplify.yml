version: 1
applications:
  - sources:
      - path: .
    frontend:
      phases:
        preBuild:
          commands:
            - cd ui
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: ui/dist/fijian-rag-app
        files:
          - '**/*'
      cache:
        paths:
          - ui/node_modules/**/*
    backend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
